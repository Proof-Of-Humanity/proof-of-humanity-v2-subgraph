{
  "name": "poh-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "gen": "graph codegen --output-dir generated/",
    "gen-goerli": "mustache config/goerli.json subgraph.template.yaml > subgraph.yaml && yarn gen",
    "gen-goerli-ext": "mustache config/goerli-ext.json subgraph.template.yaml > subgraph.yaml && yarn gen",
    "gen-opgoerli": "mustache config/opgoerli.json subgraph.template.yaml > subgraph.yaml && yarn gen",
    "gen-gnosis": "mustache config/gnosis.json subgraph.template.yaml > subgraph.yaml && yarn gen",
    "gen-mainnet-ext": "mustache config/mainnet-ext.json subgraph.template.yaml > subgraph.yaml && yarn gen",
    "deploy-goerli": "yarn gen-goerli && graph deploy --product hosted-service andreimvp/poh-goerli",
    "deploy-goerli-ext": "yarn gen-goerli-ext && graph deploy --product hosted-service andreimvp/poh-goerli-ext",
    "deploy-opgoerli": "yarn gen-opgoerli && graph deploy --product hosted-service andreimvp/poh-opgoerli",
    "deploy-gnosis": "yarn gen-gnosis && graph deploy --product hosted-service andreimvp/poh-gnosis",
    "deploy-mainnet-ext": "yarn gen-mainnet-ext && graph deploy --product hosted-service andreimvp/poh-v1-in-v2-shoes"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.33.0",
    "@graphprotocol/graph-ts": "^0.27.0",
    "mustache": "^4.2.0",
    "typescript": "^4.7.4"
  }
}
